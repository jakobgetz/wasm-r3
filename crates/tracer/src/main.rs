// extern crate tracer;
// use std::fs;

// use tracer::instrument_wasm;

// fn main() {
//     let test_name = "tests/gol";
//     let buffer = &fs::read(format!("{}.wasm", test_name)).unwrap();
//     let mut module = instrument_wasm(buffer).unwrap();
//     let _ = module.emit_wasm_file(format!("tests/instrumented.wasm"));
// }

fn main() {}